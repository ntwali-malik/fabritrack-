/**
 * Placeholder: employees API/UI can be wired here when available.
 * Not linked from the dashboard sidebar; keep for future wiring if needed.
 */
export default function EmployeePage() {
  return (
    <div className="card">
      <p className="entity-empty" style={{ padding: "1.5rem" }}>
        Employee directory is not available in this build.
      </p>
    </div>
  );
}
